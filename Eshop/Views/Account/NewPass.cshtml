@model AppUserModel

@{
    ViewData["Title"] = "Đặt lại mật khẩu";
}

<section id="form" class="account-auth">
    <div class="container">
        <div class="row">
            <div class="col-sm-6 col-sm-offset-3">
                <div class="account-auth-shell">
                    <div class="account-auth-card">
                        <span class="account-auth-badge">Reset Password</span>
                        <h1 class="account-auth-title">Tạo mật khẩu mới</h1>
                        <p class="account-auth-subtitle">Liên kết khôi phục đã được xác nhận. Hãy đặt một mật khẩu mới đủ mạnh để bảo vệ tài khoản của bạn.</p>

                        <div asp-validation-summary="ModelOnly" class="text-danger account-auth-validation"></div>

                        <form asp-action="UpdateNewPassword" asp-controller="Account" method="post" class="account-auth-form">
                            @Html.AntiForgeryToken()
                            <input type="hidden" name="token" value="@ViewBag.Token" />

                            <div class="form-group">
                                <label asp-for="Email">Email</label>
                                <input asp-for="Email" value="@ViewBag.Email" class="form-control" readonly />
                                <span asp-validation-for="Email" class="text-danger"></span>
                            </div>

                            <div class="form-group">
                                <label asp-for="PasswordHash">Mật khẩu mới</label>
                                <input asp-for="PasswordHash" type="password" class="form-control" placeholder="Nhập mật khẩu mới" />
                                <span asp-validation-for="PasswordHash" class="text-danger"></span>
                            </div>

                            <button type="submit" class="btn btn-default account-auth-submit">Cập nhật mật khẩu</button>
                        </form>

                        <div class="account-auth-links account-auth-links-single">
                            <a asp-action="Login" asp-controller="Account">Quay lại đăng nhập</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
