@model Eshop.Models.ProductOptionValueModel

@{
    ViewData["Title"] = "Thêm giá trị tùy chọn";
    var group = ViewBag.Group as Eshop.Models.ProductOptionGroupModel;
}

<div class="row g-3 option-page">
    <!-- Header -->
    <div class="col-12">
        <div class="d-flex align-items-start align-items-md-center justify-content-between gap-2 flex-column flex-md-row">
            <div>
                <h4 class="mb-1">Thêm giá trị tùy chọn</h4>
                <div class="small opacity-75">
                    Tạo giá trị cho nhóm: <strong>@group?.Name</strong>
                </div>
            </div>

            <a asp-action="Index" asp-route-productId="@group?.ProductId" class="btn btn-outline-secondary">
                <i class="fa-solid fa-arrow-left me-1"></i> Quay lại
            </a>
        </div>
    </div>

    <!-- Form -->
    <div class="col-12">
        <div class="card card-soft">
            <div class="card-body p-4">
                <form asp-action="CreateValue" method="post">
                    @Html.AntiForgeryToken()

                    <input type="hidden" asp-for="ProductOptionGroupId" />

                    <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                    <div class="row g-3">
                        <div class="col-12 col-md-6">
                            <label asp-for="Value" class="form-label">Giá trị</label>
                            <input asp-for="Value"
                                   class="form-control bg-transparent"
                                   placeholder="Ví dụ: 8GB, 16GB, 1TB" />
                            <span asp-validation-for="Value" class="invalid-feedback d-block"></span>
                        </div>

                        <div class="col-12 col-md-6">
                            <label asp-for="AdditionalPrice" class="form-label">Giá cộng thêm</label>
                            <input asp-for="AdditionalPrice"
                                   class="form-control bg-transparent"
                                   placeholder="Nhập giá cộng thêm..." />
                            <span asp-validation-for="AdditionalPrice" class="invalid-feedback d-block"></span>
                        </div>

                        <div class="col-12 col-md-6">
                            <label asp-for="Stock" class="form-label">Tồn kho</label>
                            <input asp-for="Stock"
                                   class="form-control bg-transparent"
                                   placeholder="Nhập số lượng tồn..." />
                            <span asp-validation-for="Stock" class="invalid-feedback d-block"></span>
                        </div>

                        <div class="col-12 col-md-6">
                            <label asp-for="SortOrder" class="form-label">Thứ tự hiển thị</label>
                            <input asp-for="SortOrder"
                                   class="form-control bg-transparent"
                                   placeholder="Ví dụ: 1, 2, 3..." />
                            <span asp-validation-for="SortOrder" class="invalid-feedback d-block"></span>
                        </div>

                        <div class="col-12 col-md-6">
                            <div class="form-check custom-check">
                                <input asp-for="IsDefault" class="form-check-input" />
                                <label asp-for="IsDefault" class="form-check-label">
                                    Là giá trị mặc định
                                </label>
                            </div>
                            <div class="form-text opacity-75">
                                Giá trị này sẽ được chọn sẵn khi hiển thị.
                            </div>
                        </div>

                        <div class="col-12 col-md-6">
                            <div class="form-check custom-check">
                                <input asp-for="Status" class="form-check-input" />
                                <label asp-for="Status" class="form-check-label">
                                    Hiển thị
                                </label>
                            </div>
                            <div class="form-text opacity-75">
                                Bật để giá trị này xuất hiện ngoài giao diện.
                            </div>
                        </div>
                    </div>

                    <div class="d-flex gap-2 mt-4 flex-wrap">
                        <button type="submit" class="btn btn-accent">
                            <i class="fa-solid fa-floppy-disk me-1"></i> Lưu
                        </button>

                        <a asp-action="Index" asp-route-productId="@group?.ProductId" class="btn btn-outline-secondary">
                            <i class="fa-solid fa-arrow-left me-1"></i> Quay lại
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<style>
    .option-page .card-soft {
        background: var(--main-extra-bg);
        border: 1px solid var(--border-color);
        border-radius: 1rem;
        color: var(--font-color);
        box-shadow: 0 0.125rem 0.5rem rgba(0,0,0,.04);
    }

    .option-page .form-label {
        font-weight: 600;
        margin-bottom: 0.5rem;
        color: var(--font-color);
    }

    .option-page .form-control,
    .option-page .form-select {
        background: transparent !important;
        color: var(--font-color);
        border: 1px solid var(--border-color);
        border-radius: 0.75rem;
        min-height: 44px;
    }

        .option-page .form-control:focus,
        .option-page .form-select:focus {
            border-color: var(--main-color);
            box-shadow: 0 0 0 0.2rem rgba(13, 110, 253, 0.12);
            background: transparent !important;
            color: var(--font-color);
        }

        .option-page .form-control::placeholder {
            color: rgba(255,255,255,0.45);
        }

    .option-page .custom-check {
        padding: 0.9rem 1rem;
        border: 1px solid var(--border-color);
        border-radius: 0.85rem;
        background: rgba(255,255,255,0.02);
    }

        .option-page .custom-check .form-check-input {
            margin-top: 0.2rem;
        }

        .option-page .custom-check .form-check-label {
            color: var(--font-color);
            font-weight: 500;
        }

    .option-page .btn-accent {
        background: var(--main-color);
        border: 1px solid var(--hover-border-color);
        color: var(--dark);
    }

        .option-page .btn-accent:hover {
            filter: var(--brightness);
        }

    .option-page .opacity-75 {
        color: var(--font-color);
        opacity: 0.75;
    }

    .option-page .text-danger {
        font-size: 0.875rem;
    }
</style>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}