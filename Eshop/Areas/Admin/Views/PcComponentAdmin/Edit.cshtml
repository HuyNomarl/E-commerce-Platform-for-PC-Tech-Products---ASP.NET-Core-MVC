@model Eshop.Models.ViewModels.PcComponentCreateViewModel
@using Eshop.Models.Enums
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    ViewData["Title"] = "Sửa linh kiện PC";
}

<div class="container-fluid">
    <h2 class="mb-3">Sửa linh kiện PC</h2>

    <form asp-area="Admin" asp-controller="PcComponentAdmin" asp-action="Edit" asp-route-id="@Model.Product.Id" method="post" enctype="multipart/form-data">
        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

        <div class="row g-3">
            <div class="col-lg-8">
                <div class="card mb-3">
                    <div class="card-header">Thông tin chung</div>
                    <div class="card-body">
                        <div class="mb-3">
                            <label asp-for="Product.Name" class="form-label"></label>
                            <input asp-for="Product.Name" class="form-control" />
                            <span asp-validation-for="Product.Name" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Product.Description" class="form-label"></label>
                            <textarea asp-for="Product.Description" rows="4" class="form-control"></textarea>
                            <span asp-validation-for="Product.Description" class="text-danger"></span>
                        </div>

                        <div class="row g-3">
                            <div class="col-md-4">
                                <label asp-for="Product.Price" class="form-label"></label>
                                <input asp-for="Product.Price" class="form-control" />
                                <span asp-validation-for="Product.Price" class="text-danger"></span>
                            </div>
                            <div class="col-md-4">
                                <label asp-for="Product.Quantity" class="form-label"></label>
                                <input asp-for="Product.Quantity" class="form-control" />
                                <span asp-validation-for="Product.Quantity" class="text-danger"></span>
                            </div>
                            <div class="col-md-4">
                                <label asp-for="Product.ComponentType" class="form-label">Loại linh kiện</label>
                                <select asp-for="Product.ComponentType" class="form-select" asp-items="Html.GetEnumSelectList<PcComponentType>()">
                                    <option value="">-- Chọn loại linh kiện --</option>
                                </select>
                                <span asp-validation-for="Product.ComponentType" class="text-danger"></span>
                            </div>
                        </div>

                        <div class="row g-3 mt-1">
                            <div class="col-md-6">
                                <label asp-for="Product.PublisherId" class="form-label">Thương hiệu</label>
                                <select asp-for="Product.PublisherId" asp-items="Model.Publishers" class="form-select">
                                    <option value="">-- Chọn thương hiệu --</option>
                                </select>
                                <span asp-validation-for="Product.PublisherId" class="text-danger"></span>
                            </div>
                            <div class="col-md-6">
                                <label asp-for="Product.CategoryId" class="form-label">Danh mục</label>
                                <select asp-for="Product.CategoryId" asp-items="Model.Categories" class="form-select">
                                    <option value="">-- Chọn danh mục --</option>
                                </select>
                                <span asp-validation-for="Product.CategoryId" class="text-danger"></span>
                            </div>
                        </div>

                        <div class="mt-3">
                            @if (!string.IsNullOrWhiteSpace(Model.Product.Image))
                            {
                                <div class="mb-2">
                                    <img src="~/media/products/@Model.Product.Image" style="width:120px;height:120px;object-fit:cover;border:1px solid #ddd;padding:4px;" />
                                </div>
                            }

                            <label asp-for="Product.ImageUpload" class="form-label">Ảnh sản phẩm</label>
                            <input asp-for="Product.ImageUpload" type="file" class="form-control" />
                            <span asp-validation-for="Product.ImageUpload" class="text-danger"></span>
                        </div>
                    </div>
                </div>

                <div class="card">
                    <div class="card-header">Thông số kỹ thuật</div>
                    <div class="card-body">
                        <div class="alert alert-info">
                            Với field JSON, ví dụ nhập:
                            <code>["ATX","M-ATX","ITX"]</code>
                        </div>

                        <div id="specification-container">
                            @for (int i = 0; i < Model.Specifications.Count; i++)
                            {
                                <div class="mb-3">
                                    <label class="form-label">
                                        @Model.Specifications[i].Name
                                        @if (!string.IsNullOrWhiteSpace(Model.Specifications[i].Unit))
                                        {
                                            <text> (@Model.Specifications[i].Unit)</text>
                                        }
                                    </label>

                                    <input type="hidden" asp-for="Specifications[i].SpecificationDefinitionId" />
                                    <input type="hidden" asp-for="Specifications[i].Name" />
                                    <input type="hidden" asp-for="Specifications[i].Code" />
                                    <input type="hidden" asp-for="Specifications[i].DataType" />
                                    <input type="hidden" asp-for="Specifications[i].Unit" />

                                    @if (Model.Specifications[i].DataType == SpecificationDataType.Text)
                                    {
                                        <input asp-for="Specifications[i].ValueText" class="form-control" />
                                    }
                                    else if (Model.Specifications[i].DataType == SpecificationDataType.Number)
                                    {
                                        <input asp-for="Specifications[i].ValueNumber" type="number" step="0.01" class="form-control" />
                                    }
                                    else if (Model.Specifications[i].DataType == SpecificationDataType.Boolean)
                                    {
                                        <select asp-for="Specifications[i].ValueBool" class="form-select">
                                            <option value="">-- Chọn --</option>
                                            <option value="true">Có</option>
                                            <option value="false">Không</option>
                                        </select>
                                    }
                                    else if (Model.Specifications[i].DataType == SpecificationDataType.Json)
                                    {
                                        <textarea asp-for="Specifications[i].ValueJson" rows="2" class="form-control"></textarea>
                                    }
                                </div>
                            }
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-lg-4">
                <div class="card">
                    <div class="card-header">Thao tác</div>
                    <div class="card-body d-grid gap-2">
                        <button type="submit" class="btn btn-success">Lưu thay đổi</button>
                        <a asp-area="Admin" asp-controller="PcComponentAdmin" asp-action="Index" class="btn btn-outline-secondary">Quay lại</a>
                    </div>
                </div>
            </div>
        </div>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />

    <script>
        const componentTypeSelect = document.getElementById("Product_ComponentType");
        const specContainer = document.getElementById("specification-container");

        componentTypeSelect.addEventListener("change", async function () {
            const componentType = this.value;
            if (!componentType) return;

            const response = await fetch(`/Admin/PcComponentAdmin/GetDefinitions?componentType=${componentType}`);
            const specs = await response.json();

            specContainer.innerHTML = "";

            specs.forEach((spec, index) => {
                let inputHtml = "";

                if (spec.dataType === 1) {
                    inputHtml = `<input class="form-control" name="Specifications[${index}].ValueText" />`;
                } else if (spec.dataType === 2) {
                    inputHtml = `<input type="number" step="0.01" class="form-control" name="Specifications[${index}].ValueNumber" />`;
                } else if (spec.dataType === 3) {
                    inputHtml = `
                        <select class="form-select" name="Specifications[${index}].ValueBool">
                            <option value="">-- Chọn --</option>
                            <option value="true">Có</option>
                            <option value="false">Không</option>
                        </select>`;
                } else if (spec.dataType === 4) {
                    inputHtml = `<textarea rows="2" class="form-control" name="Specifications[${index}].ValueJson"></textarea>`;
                }

                specContainer.innerHTML += `
                    <div class="mb-3">
                        <label class="form-label">
                            ${spec.name}${spec.unit ? ` (${spec.unit})` : ""}
                        </label>

                        <input type="hidden" name="Specifications[${index}].SpecificationDefinitionId" value="${spec.id}" />
                        <input type="hidden" name="Specifications[${index}].Name" value="${spec.name}" />
                        <input type="hidden" name="Specifications[${index}].Code" value="${spec.code}" />
                        <input type="hidden" name="Specifications[${index}].DataType" value="${spec.dataType}" />
                        <input type="hidden" name="Specifications[${index}].Unit" value="${spec.unit ?? ''}" />

                        ${inputHtml}
                    </div>
                `;
            });
        });
    </script>
}